const initialState = {
  users: [],
  user_count: 0,
  name: '',
  owner: '',
};

export default function chatRooms(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
};
